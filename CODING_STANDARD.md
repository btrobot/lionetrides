# Coding Standard

## 治理哲学

```
原型期（宽松） → 提交门禁（渐进） → 周期治理（集中清理）
```

| 阶段 | 策略 | 目标 |
|------|------|------|
| **原型开发** | 不打断，不阻塞 | 快速验证想法 |
| **提交前** | `pnpm gate:commit`（lint + ts-check + test） | 守住基线 |
| **周期治理** | 每 N 个功能点集中清理一次技术债 | 保持干净 |

---

## 第一部分：编码规范（必读）

### 1. TypeScript

| 规则 | 说明 | 原型期 | 提交前 |
|------|------|--------|--------|
| **禁止 `any`** | 用 `unknown` + 类型收窄，或定义 `interface` | ⚠️ 可临时用，但要加 `// TODO: type` | ❌ 零容忍 |
| **禁止未使用变量** | 声明了必须用，用不到就删 | ⚠️ 建议即时清理 | ❌ 零容忍 |
| **禁止未使用导入** | 每个 import 都要被引用 | ⚠️ 建议即时清理 | ❌ 零容忍 |
| **函数参数必须标注类型** | 禁止隐式 `any` 参数 | ⚠️ 建议标注 | ❌ 零容忍 |
| **优先 `interface`** | 对象形状用 `interface`，联合类型用 `type` | ✅ 推荐 | ✅ 推荐 |
| **优先 `const`** | 变量声明默认 `const`，只有会变的用 `let` | ✅ 推荐 | ✅ 推荐 |

### 2. React / Next.js

| 规则 | 说明 |
|------|------|
| **服务端组件优先** | 默认服务端组件，只在需要交互时加 `'use client'` |
| **`'use client'` 放叶子节点** | 尽量下推到叶子组件，不要污染整个页面 |
| **JSX 中的文本转义** | `'` → `&apos;`，`<` → `&lt;`，`>` → `&gt;` |
| **Hydration 安全** | `typeof window`、`Date.now()`、`Math.random()` 必须放在 `useEffect` + `useState` 中 |
| **禁止非法嵌套** | 如 `<p>` 嵌套 `<div>`、`<button>` 嵌套 `<button>` |

### 3. 样式

| 规则 | 说明 |
|------|------|
| **Tailwind 优先** | 使用 Tailwind 工具类，减少自定义 CSS |
| **自定义样式放 `globals.css`** | 用 `@layer` 组织：base / components / utilities |
| **颜色变量引用 Design Tokens** | 参见 `DESIGN.md` 中的色彩系统 |

### 4. 国际化（i18n）

| 规则 | 说明 |
|------|------|
| **新文本先加 `en.json`** | 以 `en.json` 为真相源 |
| **中文翻译加 `zh.json`** | 中英文同步后再运行 `pnpm sync:i18n` |
| **禁止硬编码用户可见文本** | 所有展示文本必须通过 `useTranslations()` 或 `getTranslations()` |
| **命名空间按页面划分** | 首页用 `home`，产品用 `products`，新闻用 `news`，保持模块化 |

### 5. 测试

| 规则 | 说明 |
|------|------|
| **测试金字塔** | 70% 单元 / 15% 组件 / 10% 集成 / 5% E2E |
| **禁止 `any`** | 测试中也不允许 `any`，使用 `vi.mocked()` 或正确定义类型 |
| **禁止 Mock 真实 HTTP** | 使用 MSW 或 nock |
| **禁止 `test.skip` 提交** | 临时调试可跳，提交前必须恢复 |
| **禁止 `new Date()`** | 使用 `vi.setSystemTime()` 固定时间 |

### 6. 数据库

| 规则 | 说明 |
|------|------|
| **列名用 `snake_case`** | Drizzle ORM 中 `snake_case` 列名，代码中 `camelCase` 属性 |
| **软删除** | 所有业务表包含 `deleted_at` 字段 |
| **时间戳** | 所有表包含 `created_at`、`updated_at` |

---

## 第二部分：治理流程

### 提交门禁（Pre-commit）

```bash
pnpm gate:commit    # 自动运行：lint + ts-check + test
```

- 新增代码零容忍所有 error/warning
- 存量代码不回溯（已通过集中治理清过一次基线）
- 建议配合 `husky` + `lint-staged`，只检查变更文件

### 周期集中治理

| 触发条件 | 动作 |
|----------|------|
| 每完成 3~5 个功能模块 | 跑一次 `pnpm lint`，集中清理新增问题 |
| 覆盖率低于红线 | 补测试 |
| 代码审查发现模式性问题 | 补充或收紧对应规则 |

### 规则演进

```
初始（轻量，不阻塞原型）
  → 发现问题模式（如大量 any）
  → 收紧对应规则（加入门禁）
  → 集中清理存量
  → 迭代
```

---

## 第三部分：回退策略

如果某个规则在原型期明显拖慢节奏：

1. 在该文件顶部加 `/* eslint-disable <rule> */` 并加注释说明原因
2. 在周期治理时重新评估：是规则太严还是代码需要重构
3. 如果是规则问题，下调该规则级别（error → warning）

**原则：规则服务于开发效率，不是反过来。**